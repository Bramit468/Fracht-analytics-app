# Database migrations

Store numbered Supabase SQL migrations in this directory.

The trip schema belongs to issue #3 and will be added as the first migration,
for example `0001_create_trips.sql`. Apply migrations to development before
production and never put database passwords or API keys in migration files.
